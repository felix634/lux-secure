---
import Layout from '../layouts/Layout.astro';
import SpotlightCard from '../components/ui/SpotlightCard.jsx';
import ScrambleText from '../components/ui/ScrambleText.jsx';
import { ShieldCheck, Users, Globe, Award } from 'lucide-react';
---

<Layout title="Company">
    
    <section class="max-w-4xl mx-auto text-center mb-20">
        <h1 class="text-4xl md:text-6xl font-bold text-white mb-6">
            WHO IS <span class="text-lux-green"><ScrambleText client:visible text="LUX SECURE?" /></span>
        </h1>
        <p class="text-gray-400 text-lg leading-relaxed">
            Founded in the digital shadows, we are the architects of the new internet security paradigm. 
            We do not just patch vulnerabilities; we restructure the very fabric of your digital defense.
        </p>
    </section>

    <section class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-24">
        {[
            { label: "Threats Blocked", val: "9.4B+", icon: ShieldCheck },
            { label: "Active Nodes", val: "142", icon: Globe },
            { label: "Security Analysts", val: "50+", icon: Users },
            { label: "Global Awards", val: "12", icon: Award }
        ].map(stat => (
            <SpotlightCard client:visible className="p-6 text-center">
                <stat.icon className="w-8 h-8 text-lux-green mx-auto mb-4" />
                <h3 class="text-3xl font-bold text-white mb-1">{stat.val}</h3>
                <p class="text-xs font-mono text-gray-500 uppercase tracking-widest">{stat.label}</p>
            </SpotlightCard>
        ))}
    </section>

    <section class="mb-24 flex flex-col md:flex-row gap-12 items-center">
        <div class="flex-1">
            <h2 class="text-2xl font-bold text-white mb-4 border-l-4 border-lux-green pl-4">THE MISSION</h2>
            <p class="text-gray-400 mb-6 leading-relaxed">
                In an era where data is the new currency, protection is not a luxury—it is a necessity. 
                Lux Secure was built on the principle of <span class="text-lux-green">"Zero Trust, Absolute Verification."</span>
            </p>
            <p class="text-gray-400 leading-relaxed">
                We combine military-grade encryption standards with proprietary AI heuristics to predict attacks 
                before they execute. Our team consists of former intelligence officers, white-hat hackers, 
                and quantum cryptography researchers.
            </p>
        </div>
        <div class="flex-1 w-full">
            <div class="w-full h-64 bg-lux-green/5 border border-lux-green/20 rounded-sm relative overflow-hidden flex items-center justify-center">
                <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(0,255,65,0.1),transparent)]"></div>
                <div class="text-lux-green font-mono text-xs text-center">
                    <div class="mb-2 animate-pulse">[ SECURE FACILITY ]</div>
                    <div class="opacity-50">Vienna, Austria HQ</div>
                </div>
            </div>
        </div>
    </section>

</Layout>