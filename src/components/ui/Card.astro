---
const { title, class: className } = Astro.props;
---
<div class={`lux-glass p-6 rounded-sm border-t border-t-lux-green/30 hover:border-lux-green/60 transition-colors duration-300 ${className}`}>
    {title && <h3 class="text-xl font-bold text-white mb-4 lux-neon-text">{title}</h3>}
    <slot />
</div>